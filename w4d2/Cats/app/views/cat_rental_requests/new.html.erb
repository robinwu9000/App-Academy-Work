<!DOCTYPE html>
<html>
  <body>
    <h1>Make a rental request.</h1>
    <form action="<%= cat_rental_requests_url %>" method="post">
      <label for="crr">Pick a cat</label>
      <select name="cat_rental_request[cat_id]" id="crr">
        <option value="" selected="selected"></option>
        <% Cat.all.each do |cat| %>
          <option value=<%= cat.id %> > <%= cat.name %> </option>
        <% end %>
      </select>
      <br>
      <br>

      <label for="start">Start date</label>
      <input type="date" name="cat_rental_request[start_date]" id="start">
      <br>
      <br>

      <label for="end">End date</label>
      <input type="date" name="cat_rental_request[end_date]" id="end">
      <br>

      <input type="submit" value="Make Request">
  </body>
</html>
